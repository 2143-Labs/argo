apiVersion: v1
kind: Service
metadata:
  name: immich
  labels:
    app: immich
spec:
  # No selector — routes via manual Endpoints to nas.ts.2143.me:2283
  ports:
    - port: 80
      targetPort: 2283
      protocol: TCP
      name: http
