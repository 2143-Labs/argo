apiVersion: v1
kind: Service
metadata:
  name: litellm-postgres
  labels:
    app: litellm-postgres
spec:
  clusterIP: None
  selector:
    app: litellm-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
