apiVersion: v1
kind: Service
metadata:
  name: litellm-redis
  labels:
    app: litellm-redis
spec:
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  selector:
    app: litellm-redis
  type: ClusterIP
