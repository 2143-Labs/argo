apiVersion: v1
kind: Service
metadata:
  name: heorot
  namespace: matrix
  labels:
    app.kubernetes.io/name: heorot
    app.kubernetes.io/instance: heorot
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/name: heorot
    app.kubernetes.io/instance: heorot
  type: ClusterIP
