apiVersion: apps/v1
kind: Deployment
metadata:
  name: frigate-genai-ollama
  labels: {app: frigate-genai-ollama}
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy: {type: Recreate}
  selector:
    matchLabels: {app: frigate-genai-ollama}
  template:
    metadata:
      labels: {app: frigate-genai-ollama, app.kubernetes.io/part-of: frigate-genai}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        linkerd.io/inject: enabled
        config.linkerd.io/skip-outbound-ports: "7233"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: frigate-genai
      terminationGracePeriodSeconds: 180
      containers:
        - name: worker
          image: ghcr.io/john2143/frigate-genai-genai:v86
          args: ["--mode=genai-ollama"]
          envFrom:
            - secretRef: {name: frigate-genai-worker-creds}
          env:
            - name: S3_ACCESS_KEY
              valueFrom:
                secretKeyRef: {name: frigate-genai-worker-creds, key: AWS_ACCESS_KEY_ID}
            - name: S3_SECRET_KEY
              valueFrom:
                secretKeyRef: {name: frigate-genai-worker-creds, key: AWS_SECRET_ACCESS_KEY}
            - {name: TEMPORAL_ADDRESS, value: "temporal-frontend.default.svc.cluster.local:7233"}
            - {name: TEMPORAL_TLS, value: "true"}
            - {name: TEMPORAL_TLS_CA, value: "/etc/temporal-certs/ca.crt"}
            - {name: TEMPORAL_WORKER_BUILD_ID, value: "v86"}
            - {name: TEMPORAL_DEPLOYMENT_NAME, value: "frigate-genai-ollama"}
          resources:
            requests: {cpu: "250m", memory: "512Mi"}
            limits: {cpu: "1000m", memory: "2Gi"}
          ports:
            - {containerPort: 8080, name: http}
          startupProbe:
            httpGet: {path: /healthz, port: 8080}
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 12
          readinessProbe:
            httpGet: {path: /readyz, port: 8080}
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet: {path: /healthz, port: 8080}
            periodSeconds: 30
            failureThreshold: 3
