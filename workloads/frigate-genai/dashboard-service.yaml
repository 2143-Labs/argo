apiVersion: v1
kind: Service
metadata:
  name: frigate-genai-dashboard
  labels: {app: frigate-genai-dashboard}
spec:
  type: ClusterIP
  selector: {app: frigate-genai-dashboard}
  ports:
    - port: 9090
      targetPort: 8080
      protocol: TCP
      name: http
