apiVersion: v1
kind: Service
metadata:
  name: steam-lobby
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: steam-lobby
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
