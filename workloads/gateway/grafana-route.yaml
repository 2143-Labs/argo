apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  namespace: observability
spec:
  parentRefs:
    - name: shared-gateway
      sectionName: grafana-ts-2143-https
  hostnames: [grafana.ts.2143.me]
  rules:
    - backendRefs:
        - name: grafana
          port: 3000
