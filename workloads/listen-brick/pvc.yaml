apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: listen-brick-data
  namespace: default
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn-2
  resources:
    requests:
      storage: 2Gi
