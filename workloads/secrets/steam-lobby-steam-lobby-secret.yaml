# Rendered into namespace steam-lobby as Secret/steam-lobby-secret from OpenBao
# consumers/data/john2143-com/steam-lobby/steam-lobby-secret. Consumed by workloads unchanged -
# target.name must keep matching the name the Deployment already references.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: steam-lobby-secret
  namespace: steam-lobby
spec:
  refreshInterval: 10m
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: steam-lobby-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: john2143-com/steam-lobby/steam-lobby-secret
