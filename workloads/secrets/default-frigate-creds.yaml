# Rendered into namespace default as Secret/frigate-creds from OpenBao
# consumers/data/john2143-com/default/frigate-creds. Consumed by workloads unchanged -
# target.name must keep matching the name the Deployment already references.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: frigate-creds
  namespace: default
spec:
  refreshInterval: 2m
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: frigate-creds
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: john2143-com/default/frigate-creds
