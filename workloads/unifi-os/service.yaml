apiVersion: v1
kind: Service
metadata:
  name: unifi-os
  namespace: default
  labels:
    app: unifi-os
spec:
  # No selector — routes via EndpointSlice to the UniFi OS Server VM at 192.168.5.30:11443
  ports:
    - port: 11443
      targetPort: 11443
      protocol: TCP
      name: https
