apiVersion: v1
kind: Service
metadata:
  name: aross-studio
  labels:
    app: aross-studio
spec:
  type: ClusterIP
  selector:
    app: aross-studio
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
