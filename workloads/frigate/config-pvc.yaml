apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-config
  namespace: default
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: longhorn-3
  resources:
    requests:
      storage: 20Gi
