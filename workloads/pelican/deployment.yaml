# Pelican panel: single container (official ghcr.io/pelican-dev/panel image).
# supervisord inside runs php-fpm + queue-worker + supercronic (scheduler);
# Caddy serves :80 HTTP-only because BEHIND_PROXY=true (traefik terminates
# TLS). The entrypoint runs `artisan migrate --force` on start when
# APP_INSTALLED=true, so no separate init/worker/scheduler workloads.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pelican
  namespace: default
  labels:
    app: pelican
spec:
  replicas: 0
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: pelican
  template:
    metadata:
      labels:
        app: pelican
    spec:
      securityContext:
        fsGroup: 82
        fsGroupChangePolicy: OnRootMismatch
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              preference:
                matchExpressions:
                  - key: workload-type
                    operator: In
                    values:
                      - general
      containers:
        - name: panel
          image: ghcr.io/pelican-dev/panel:v1.0.0-beta38
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              protocol: TCP
              name: http
          envFrom:
            - configMapRef:
                name: pelican-config
            - secretRef:
                name: pelican-env
          env:
            # Explicit env wins over envFrom. CNPG owns this credential: it
            # generates the password for the managed role and can rotate it.
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pelican-db-app
                  key: password
          volumeMounts:
            - mountPath: /pelican-data
              name: pelican-data
            # plugins live under the data volume (compose uses subpath)
            - mountPath: /var/www/html/plugins
              name: pelican-data
              subPath: plugins
            - mountPath: /var/www/html/storage/logs
              name: pelican-logs
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /login
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 6
          readinessProbe:
            httpGet:
              path: /login
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 6
      volumes:
        - name: pelican-data
          persistentVolumeClaim:
            claimName: pelican-data-lh
        - name: pelican-logs
          persistentVolumeClaim:
            claimName: pelican-logs-lh
