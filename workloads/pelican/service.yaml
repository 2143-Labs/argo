# v4-only ClusterIP backing the pelican HTTPRoute (frigate-http pattern).
apiVersion: v1
kind: Service
metadata:
  name: pelican-http
  namespace: default
spec:
  type: ClusterIP
  selector: {app: pelican}
  ports:
    - {name: http, port: 80, targetPort: 80}
