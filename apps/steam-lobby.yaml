apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: steam-lobby
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/2143-Labs/argo.git
    targetRevision: HEAD
    path: workloads/steam-lobby
  destination:
    server: https://kubernetes.default.svc
    namespace: steam-lobby
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
